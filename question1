#include<iostream>
using namespace std;
int power(int,int);
int main()
{
int a,b,result;
cout<<"enter a base and it's exponent respectievely"<<endl;
cin>>a>>b;
result=power(a,b);
cout<<"the result is "<<result<<endl;
}
int power(int a,int b)
{
if(b==1)
return 1;
else 
return (a*power(a,b-1));
}

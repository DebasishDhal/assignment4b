#include<iostream>
using namespace std;
int factorial(int n);
int main()
{
int n;
cout<<"enter a number to find out it's factorial"<<endl;
cin>>n;
cout<<"the factorial of number is "<<factorial(n)<<endl;
return 0;
}
int factorial(int n)
{
if(n!=1)
return (n*factorial(n-1));
else 
return 1;
}
